/**
 * //any타입
 * 어떤 타입이든 가능함. 그러나 지양해야 함. 이걸 쓰면 자바스크립트랑 다른게 없음 
 * 만약 어떤 타입이 any라면 타입 표기를 직접 해주는게 좋다.
 * any타입 배열은 push마다 추론되는 타입이 바뀐다. 그리고 concat못 쓴다.
 * any타입 변수는 숫자나 문자열 타입 연산에서 타입이 바뀌기도 한다.
 * //
 */
